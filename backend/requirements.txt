# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Data validation
pydantic==2.10.4
pydantic[email]==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# HTTP / async
httpx==0.28.1
python-multipart==0.0.20

# WebSocket
websockets==13.1

# Caching
redis==5.2.1

# Email
aiosmtplib==3.0.2

# Batch pipeline (R10) — APScheduler kept for import compatibility; jobs now run via Celery Beat
apscheduler==3.10.4

# WSGI server (multi-worker, --max-requests for memory-leak prevention)
gunicorn==22.0.0

# Task queue
celery[redis]==5.4.0
flower==2.0.1

# Full-text search
meilisearch==0.31.4

# Object storage (MinIO/S3-compatible avatar store — Phase 3)
boto3==1.35.0

# Observability / Prometheus metrics (R12)
prometheus-client==0.21.0

# OpenTelemetry tracing → Jaeger (Phase 3)
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp-proto-grpc==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-instrumentation-sqlalchemy==0.46b0
opentelemetry-instrumentation-redis==0.46b0

# Utilities
python-dotenv==1.0.1
